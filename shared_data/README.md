# shared_data (READ-ONLY)

## Sample tier (current)

- `sample_master_registration_data.xlsx` — registry / master (~50 rows in sample)
- `sample_transaction_supply_data.xlsx` — transaction / supply log (~2,400 rows in sample)

## Production tier (future)

PM may replace these files or designate new basenames. Agents must adapt via dynamic ingestion (see `DATA_LAYER.md`).

**No agent may write to this directory.**
