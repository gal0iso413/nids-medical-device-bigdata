"""
Streamlit entrypoint shell — Class 1.
Phase 1 locked: implement EDA and pipelines only after PM approval in Composer.
"""
from __future__ import annotations

import streamlit as st

st.set_page_config(page_title="Class 1 — Anomaly Detection", layout="wide")
st.title("Class 1 — Distribution Anomaly Detection")
st.info(
    "Phase 1 (Autonomous EDA) is active. Modeling and UI implementation are "
    "deferred until the PM approves the next phase in Cursor Composer."
)
