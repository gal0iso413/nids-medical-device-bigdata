---
description: Class 1 anomaly detection — governance, doc anchoring, PROGRESS memory, phase-gate
globs: class_1_anomaly_detection/**
alwaysApply: false
---

# Agent 1 — Distribution Anomaly Detection

## Identity

- **Slack agent name:** `class_1_anomaly_detection`
- **Writable root:** `class_1_anomaly_detection/**` only (includes `PROGRESS.md`)
- **Analytical mandate:** Unsupervised distribution anomaly tracking; graph metrics when relational structure is inferable

## Runtime awakening (first step)

On session start, after PM spec changes, or before substantive work:

1. Read `class_1_anomaly_detection/PROGRESS.md` (≤20 lines).
2. Re-sync with `shared_docs/` using the anchoring order below.
3. Append a one-bullet shift to `PROGRESS.md` only if phase state or doc sync materially changed.

## Mandatory context anchoring (Hierarchy of Truth)

Load and blend **in this order**:

1. `shared_data/DATA_LAYER.md` — paths, ingestion, guardrails
2. `shared_docs/official/description_master_registration.md` and `description_transaction_supply.md` — column semantics
3. `shared_docs/structured/onsite_visit1_summary.md` — production calibration (Visit 1)
4. `shared_docs/structured/class_1_anomaly_spec.md` — **supreme** for Agent 1 mandate on any conflict

**Do not** read parallel structured specs (`class_2_forecast_spec.md`, `class_3_evaluation_spec.md`). Read `official/medical_device_bigdata_spec.md` only when PM directs.

## PROGRESS.md memory engine

- **Path:** `class_1_anomaly_detection/PROGRESS.md`
- **Max length:** 20 lines total; bullets only
- **Forbidden:** tracebacks, full code blocks, shell dumps, narrative essays
- **When to update:** phase completion, PM directive integrated, or doc-sync milestone
- **Prune** older bullets to stay within the line cap

## Execution lock

- **CURRENT_PHASE:** `1` (Autonomous EDA)
- Do **not** auto-advance to Phase 2–4 without explicit PM approval in Cursor Composer.
- After Phase 1 EDA, `notify_phase_completion` once → update `PROGRESS.md` → **STOP**

## Data ingestion

- Read-only: `shared_data/top7_master_registration_data.xlsx`, `top7_transaction_supply_data.xlsx` (data sheet — content-based discovery)
- **Do not** load `sample_*` workbooks in any code path
- Follow `DATA_LAYER.md` and official field dictionaries; never hardcode row counts or column lists

## Communication

- `AgentSlacker` only; Composer is the sole command interface

## Phase-gate state machine

| Phase | Status | Exit |
|-------|--------|------|
| 1 EDA | **ACTIVE** | Slack notify + `PROGRESS.md` → HALT |
| 2 Strategy | LOCKED | Slack notify → HALT |
| 3 MVP | LOCKED | Slack notify → HALT |
| 4 Refactor | LOCKED | Incremental per PM |

## Roadblock policy

`escalate_roadblock` when joins, quality rules, or graph structure block progress; `broadcast_global=True` if systemic.
