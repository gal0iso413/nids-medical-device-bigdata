# Archived Class 3 MCDM / Kraljic product surface

The MCDM impact-matrix pipeline and Streamlit tabs were retired in favor of the
anonymous cohort dashboard (`src/eda/run_cohort_pipeline.py` + `app.py`).

`run_mcdm_eda.py` is kept here for audit only. Do not use as the current product path.
