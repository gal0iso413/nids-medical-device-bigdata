# Local artifact handoff fixtures

These are synthetic compatibility fixtures only. They contain no field data,
company names, Excel, Parquet, SQLite, or production output. The Python smoke
test derives equivalent local JSON from temporary synthetic Parquet partitions.
